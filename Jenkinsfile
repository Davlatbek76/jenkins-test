pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo '✅ Salom, bu Jenkins pipeline!'
            }
        }

        stage('Build') {
            steps {
                echo '🚧 Bu yerda docker build yoki test bo‘lishi mumkin.'
            }
        }
    }
}
